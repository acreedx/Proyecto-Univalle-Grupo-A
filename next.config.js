/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['source.unsplash.com','flowbite.s3.amazonaws.com','www.youtube.com'],
      },
}

module.exports = nextConfig
